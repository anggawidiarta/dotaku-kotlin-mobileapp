<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.picodiploma.dotaku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dotaku">
        <activity android:name=".activities.activity.ListLore"></activity>
        <activity android:name=".activities.activity.LoreActivity" />
        <activity android:name=".activities.activity.StrengthActivity" />
        <activity android:name=".activities.activity.LaporanActivity" />
        <activity android:name=".activities.activity.ListHeroActivity" />
        <activity android:name=".activities.activity.DetailHeroActivity" />
        <activity android:name=".activities.activity.ProfilActivity" />
        <activity android:name=".activities.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>